
* make dashboard list view a table, populate with different fields, last updated, etc
* make data/detail topic views on a right sidebar?
* input for topic data view
    * picklist for user
    * subjects y, entries x
    * allow addition of subjects and entries
    * allow renaming of subjects (and entries?)
    * allow renaming for user (button to right of picklist)
* topic plots 
    * choose the plotting library
* dashboard previews
* settings page
* connect to backend
* login
